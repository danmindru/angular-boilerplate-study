absConfig.pushAfterBootstrap('abs.feature.profile.providerPage');

angular.module('abs.feature.profile.providerPage').config(providerPageConfig);

function providerPageConfig(){
}

providerPageConfig.$inject = [];