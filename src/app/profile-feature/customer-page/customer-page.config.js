absConfig.pushAfterBootstrap('abs.feature.profile.customerPage');

angular.module('abs.feature.profile.customerPage').config(customerPageConfig);

function customerPageConfig(){
}

customerPageConfig.$inject = [];